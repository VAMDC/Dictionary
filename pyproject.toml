[project]
name = "vamdc-dictionary"
version = "1.0.0"
description = "The VAMDC dictionary: a browsable keyword database plus helper tools for node setup"
requires-python = ">=3.14"
dependencies = [
    "django>=5.2,<6.0",
    "gunicorn>=23.0",
]

[tool.uv]
package = false
